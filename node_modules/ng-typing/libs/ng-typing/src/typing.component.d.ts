import { OnInit, EventEmitter } from '@angular/core';
export declare class TypingComponent implements OnInit {
    static readonly MAX_TYPO_PROBABILITY: number;
    static readonly TYPO_FIX_SPEED: number;
    static readonly RANDOM_CHARACTERS: string;
    message: string;
    referenceSpeed: number;
    speedDeviation: number;
    typo: boolean;
    typoProbability: number;
    onCompleted: EventEmitter<void>;
    chrs: string[];
    position: number;
    displayedChrs: string;
    progress: boolean;
    ngOnInit(): void;
    show(message?: string): void;
    private initParameters();
    private displayCharacter();
    private getRandomCharacter();
    private correctTypo();
    private randomSpeed();
    private doTypo();
}
